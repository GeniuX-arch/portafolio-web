---
interface Props {
  name: string;
  link: string;
}

const { name, link } = Astro.props;
---


<div class="h-20 text-center bg-black mx-10 hover:text-purple-500 transition duration-150">
    <a href={link}>{name}</a>
</div>